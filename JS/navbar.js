/**
 * @deprecated Подключайте JS/main.js (type="module").
 * Файл оставлен для совместимости — перенаправляет на модульную версию.
 */
import { initNavbar } from "./components/navbar.js";

initNavbar();
